# Frontend (Phase 1 placeholder)

No UI is implemented yet. This folder only holds the route/component/state
structure defined in the approved architecture, so later phases can drop
implementation into the right place without restructuring first.

See `docs/architecture/README.md` for what each route is for.
